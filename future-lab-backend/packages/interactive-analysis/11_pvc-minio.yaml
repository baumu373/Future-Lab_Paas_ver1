apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-USER_ID_PLACEHOLDER-minio
  labels:
    user-id: USER_ID_PLACEHOLDER
spec:
  accessModes: [ "ReadWriteOnce" ]
  resources: { requests: { storage: "5Gi" } }